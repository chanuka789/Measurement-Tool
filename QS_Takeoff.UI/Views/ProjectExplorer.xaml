<UserControl x:Class="QS_Takeoff.UI.Views.ProjectExplorer"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <!-- Dimension groups section -->
        <TextBlock Text="Dimensions" FontWeight="Bold" Margin="5" />
        <ListBox Grid.Row="1" ItemsSource="{Binding DimensionGroups}" Margin="5" />

        <!-- Drawing section -->
        <TextBlock Grid.Row="2" Text="Drawings" FontWeight="Bold" Margin="5" />

        <TextBox x:Name="FilterBox" Grid.Row="3" Margin="5" Text="Filter" Foreground="Gray"
                 GotFocus="FilterBox_GotFocus" LostFocus="FilterBox_LostFocus" />

        <ListBox Grid.Row="4" ItemsSource="{Binding Drawings}" Margin="5" />
    </Grid>
</UserControl>
